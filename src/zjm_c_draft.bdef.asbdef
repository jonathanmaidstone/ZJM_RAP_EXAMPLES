projection;
strict ( 2 );
use draft;
define behavior for Zjm_C_Draft //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action Activate;
}